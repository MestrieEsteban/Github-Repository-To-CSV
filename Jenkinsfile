pipeline {
    agent any
	environment {
		nom = 'toto',
		couleur = 'rouge'
	}
    stages {
        stage('Lister les variables') {
            steps {
                echo 'Lister..'
            }
        }
    }
}
